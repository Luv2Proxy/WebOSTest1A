export class WindowServer {constructor(gui){this.gui=gui;this.drag=null;}pointerDown(x,y){for(let i=this.gui.windows.length-1;i>=0;i--){const w=this.gui.windows[i];if(x>=w.x&&x<=w.x+w.w&&y>=w.y&&y<=w.y+32){this.gui.focus(w.id);this.drag={id:w.id,dx=x-w.x,dy=y-w.y};return true;}}return false;}pointerMove(x,y){if(!this.drag)return;const w=this.gui.windows.find(v=>v.id===this.drag.id);if(w){w.x=x-this.drag.dx;w.y=y-this.drag.dy;this.gui.render();}}pointerUp(){this.drag=null;}clickClose(x,y){for(let i=this.gui.windows.length-1;i>=0;i--){const w=this.gui.windows[i];if(x>=w.x+w.w-34&&x<=w.x+w.w-12&&y>=w.y+6&&y<=w.y+26){this.gui.closeWindow(w.id);this.gui.render();return true;}}return false;}}